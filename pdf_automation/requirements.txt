# PDF reading and manipulation
pymupdf==1.24.5          # fitz - reads/writes PDF preserving layout exactly
pypdf==4.3.1             # fallback PDF reader

# Font and text tools
Pillow==10.4.0           # image handling for placeholder replacement

# Data and config
pyyaml==6.0.1            # project data files in YAML format
colorama==0.4.6          # colored terminal output for progress

# Optional: source doc extraction (if user provides .docx/.pptx)
python-docx==1.1.2       # extract text from .docx files
python-pptx==0.6.23      # extract text from .pptx files
pdfplumber==0.11.3       # extract text from source PDF files
